import ChallengeForm from "../components/ChallengeForm";

export const CreateChallenge = () => {
    return (
        <div className="max-w-2xl mx-auto">
            <h1 className="text-xl font-bold">Create the Challenge to Save the World!</h1>
            <ChallengeForm />
        </div>
    );
};

